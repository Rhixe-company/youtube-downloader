# Repository Summary — youtube-downloader

> Generated by `/repo-story-time` from real git history. All facts below are evidence-based from `git log` and the repository tree as of 2026-07-16.

## Overview
`youtube-downloader` is a **Python CLI utility** for downloading YouTube content (single video, playlist, and loop/batch modes) using `yt-dlp` + `curl_cffi`. It is a pure command-line tool — no web UI, no API. The repo holds **5 commits**, all by `rhixecompany`, dated **2026-06-12 → 2026-07-16**. Post-initial commits are research/tooling updates, matching the workspace pattern.

**Sole contributor:** `rhixecompany <rhixecompany@gmail.com>` (5 commits, 100%).

## Architecture
- **Type:** Single-purpose Python CLI
- **Pattern:** One script per mode — `main_noplaylist.py` (single), `main_playlist.py` (playlist), `main_loop_noplaylist.py` / `main_loop_playlist.py` (batch loop), `test.py` (tests)
- **Engine:** `yt-dlp` (download) + `curl_cffi` (HTTP client, helps bypass some bot protections)
- **Media processing:** FFmpeg (external, for conversions/post-processing)
- **Reference docs:** `docs/Project_Architecture/` (Workflow Analysis, exemplars); `docs/youtube-downloader-triage-context.md`

## Key Components
| Area | Evidence (paths) |
| --- | --- |
| Entry scripts | `main_noplaylist.py`, `main_playlist.py`, `main_loop_noplaylist.py`, `main_loop_playlist.py`, `test.py` |
| Docs | `README.md`, `RESEARCH_REPORT.md`, `docs/SETUP.md`, `docs/youtube-downloader-triage-context.md`, `web-research-youtube-downloader.md` |
| Governance | `LICENSE`, `CODE_OF_CONDUCT.md`, `CONTRIBUTING.md`, `.github/ISSUE_TEMPLATE/`, `.github/PULL_REQUEST_TEMPLATE/` |
| Deps | `requirements/base.txt`, `requirements/local.txt` |
| Tooling | `.vscode/` (5 configs), `.github/copilot-instructions.md` |

The repo contains **29 tracked files** (excluding `.git`/`node_modules`); **~228 lines of Python** across the five scripts.

## Technologies
- **Language:** Python 3.x
- **Core libs:** `yt-dlp` (latest), `curl_cffi` (latest)
- **External:** FFmpeg (format conversions)
- **Quality:** `ruff` (lint), `mypy` (optional type check)
- **Conventions (from AGENTS.md):** keep `yt-dlp` updated for site compatibility; add delays between requests to respect rate limits; URLs/`.env` never committed; test via `python test.py`.

## Data Flow
1. User runs a mode script (single / playlist / loop).
2. Script builds a `yt-dlp` request using `curl_cffi` as the HTTP client (bot-protection bypass).
3. `yt-dlp` fetches video/audio; polite delays honored between requests (rate limiting).
4. FFmpeg post-processes/conversts the downloaded media.
5. `test.py` validates behavior.

## Team
- **Single maintainer.** `git shortlog -sn` lists only `rhixecompany`. No co-authors or reviewed PRs.
- Cadence: initial drop (Jun 12) + config/research commits (Jun 25, Jun 30, Jul 10, Jul 16).

## Honest Caveat
History is **sparse (5 commits, no feature commits)**. Architecture is drawn from `README.md`, `AGENTS.md`, and the script filenames, not incremental code history. Notably, this repo *does* ship a `LICENSE`, `CODE_OF_CONDUCT.md`, and GitHub issue/PR templates — more open-source scaffolding than its siblings — even though no external contributions appear in git history.
