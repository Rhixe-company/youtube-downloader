# Audit Report: youtube-downloader

> Read-only repo-management audit (Phases 0, 2, 3). Destructive phases (1: branch deletion/push, 4: CI creation) HELD for user approval. Generated by focused subagent.

## Overview
- **Type:** Python CLI tool for downloading YouTube videos (single / playlist / loop mode).
- **Stack:** Python 3.x + yt-dlp + curl_cffi + FFmpeg (external). No web framework. Optional ruff/mypy.
- **AGENTS.md present:** Yes (43 lines, accurate).
- **README.md present:** Yes (clear usage, security notes).

## Disk Usage
- **Source (excl. .git, node_modules, dist, build):** 232K (28 git-tracked files).
- **node_modules:** none (pure Python).
- No venv present locally.

## Entrypoint
- **CLI scripts:** `main_noplaylist.py` (single), `main_playlist.py` (playlist), `main_loop_playlist.py` (batch loop), `test.py`.
- **Run:** `python main_noplaylist.py` etc. Install: `pip install -r requirements/base.txt` (or `pip install yt-dlp curl_cffi`).
- All scripts use `from __future__ import unicode_literals` (Py2-compat shim, harmless on Py3).

## Gitignore Audit
File exists (4665 bytes — comprehensive Toptal "windows + vscode + python" generator).
- ✅ `.env`, ✅ `__pycache__/`, ✅ `*.py[cod]`, ✅ `dist/`, ✅ `build/`, ✅ `venv/`/`env/`/`.venv/`/`ENV/`, ✅ `.idea/`, ✅ `*.vsix`, ✅ `Thumbs.db`, ✅ `*.log`.
- ⚠️ **Missing `.DS_Store`** — minor (macOS-only; Windows/VSCode sections present but no `.DS_Store` line). Cross-platform hygiene gap.
- ⚠️ `node_modules/` not listed — **N/A** (no Node in this project).
- ⚠️ `.next/` N/A.
- **Verdict:** ✅ Strong for a Python CLI. Only `.DS_Store` omission is noteworthy (low severity).

## Dependency Audit
- **Manifest:** `requirements/base.txt` (present, 60 lines, versioned with `>=` constraints) + `requirements/` dir. No `pyproject.toml`/`Pipfile`.
- **pip list / pip-audit availability:** `pip` 26.1.2 available; `pip-audit` NOT on PATH. Not executed (deps not installed here; read-only).
- **Contents:** yt-dlp-related deps are NOT in this file — instead it lists sphinx, Faker, djlint, black, ruff, coverage, pre-commit, virtualenv, uvicorn, starlette, etc.
- **Flags / known-bad:**
  - ⚠️ **Mismatch:** `requirements/base.txt` appears to be a **generic/auto-generated Python requirements file** (docs tooling: sphinx, djlint, black, Faker, factory-boy) — it does **NOT** contain `yt-dlp` or `curl_cffi` which the README/AGENTS.md cite as the core deps. Either the real deps are unmanaged, or the wrong requirements file is committed. This needs reconciliation.
  - ⚠️ Constraints use `>=` (e.g. `requests>=2.32`) — lower-bound only, non-reproducible upper bounds.
  - ℹ️ `virtualenv` listed as a runtime requirement is unusual (it's a dev tool).
- **Recommendation:** Create a correct `requirements.txt` with `yt-dlp` + `curl_cffi` (+ FFmpeg note), pin versions, add `pip-audit` to CI, and ignore `.DS_Store`.

## Branch State
- **Local branches:** `* development` (current), `production`.
- **Remote branches:** `origin/HEAD -> origin/production`, `origin/development`, `origin/master`, `origin/production`.
- ⚠️ **`master` branch exists (remote)** — non-standard branch.
- **origin/HEAD** points to `production`.

## Destructive Phases HELD
- ⛔ **Phase 1 (branch deletion / push):** NOT executed. `master` NOT deleted, no pushes.
- ⛔ **Phase 4 (create CI):** NOT executed.
- Read-only except this AUDIT report.
